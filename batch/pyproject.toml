[project]
name = "batch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=7.2.0",
    "pyspark>=4.1.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
